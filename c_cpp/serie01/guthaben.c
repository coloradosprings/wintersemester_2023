#include <stdio.h>

int main() {
double guthaben = 0;
scanf("%d",guthaben);
double guthaben 60/(0.24 * guthaben);
printf("restliche Minuten %d")
}

// Aufgabe 1.4. Bei einem aktuellen Smartphone-Tarif kostet eine Minute Telefonieren in ein beliebiges
// Mobilfunknetz nach Verbrauch der Freiminuten 0.27 e. Schreiben Sie ein Programm, das ein gewisses
// Guthaben g ∈ R von der Tastatur einliest und ausgibt wie lange (Minuten) man damit telefonieren kann.
// Speichern Sie den Source-Code unter handy.c in das Verzeichnis serie01.